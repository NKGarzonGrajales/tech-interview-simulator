
{
  "name": "tech-interview-simulator",
  "version": "1.0.0",
  "description": "MindForge - Technical Interview Simulator",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r --parallel run build",
    "start": "pnpm -r --parallel run start",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}